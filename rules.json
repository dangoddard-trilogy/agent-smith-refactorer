{
  "prompt": "`{rerun_notice}` Using Code Interpreter, refactor the Java code in `{file_id}` by updating deprecated `{shorthand_method_name}` (imported as `{deprecated_method}`) to their Spring 5.x equivalents. Ensure changes are precise, avoiding alterations to unrelated class/method names and preserving original code functionality.",
  "instructions": [
    {
      "step": "Identify Deprecated Usage",
      "detail": "Find all occurrences of `{shorthand_method_name}`, excluding imports."
    },
    {
      "step": "Plan Refactoring",
      "detail": "For each occurrence, determine the Spring 5.x replacement."
    },
    {
      "step": "Execute Refactoring",
      "detail": "Replace deprecated usage with Spring 5.x equivalents, excluding related imports."
    },
    {
      "step": "Review and Verify",
      "detail": "Ensure all instances (excluding imports) are updated and the refactored code functions as intended. Finally, ensure imports are updated to match the changes."
    }
  ],
  "conclusion": "Follow these steps to effectively upgrade the code, keeping its functionality intact."
}
